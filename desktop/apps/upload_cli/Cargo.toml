[package]
name = "upload_cli"
version = "1.0.0"
edition = "2024"

[dependencies]
indicatif = "0.18"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "signal"] }
upload = {path = "../../packages/upload"}
